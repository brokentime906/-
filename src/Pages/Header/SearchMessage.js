import React from "react";
import { Input } from "semantic-ui-react";

const SearchMessage = () => {
  return (
    <>
      <Input icon="search" color="violet" inverted />
    </>
  );
};

export default SearchMessage;
